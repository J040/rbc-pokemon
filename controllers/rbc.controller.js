module.exports = function(router, graph) {
    // GET
    router.get('/batalha/:p1/:p2', );

    function seguir(request, response) {
        let p1 = +request.params.p1;
        let p2 = +request.params.p2;
    }
}